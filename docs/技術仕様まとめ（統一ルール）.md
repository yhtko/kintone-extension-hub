kintone Base Launcher
技術仕様まとめ（統一ルール中心）
1. 全体アーキテクチャ

この拡張は Chrome Extension + Side Panel UI を前提としている。

構成
chrome_kintone_favorites
│
├ sidepanel.html        # サイドパネルUI
├ launcher.js           # ランチャーUIロジック
├ launcher.css          # ランチャースタイル
│
├ content.js            # kintoneページ拡張
├ permission-service.js # 権限判定サービス
├ page-bridge.js        # ページ通信
├ overlay.css           # Excel overlay UI
│
├ service_worker.js     # background service worker
│
├ options.html          # 設定画面
├ options.js
├ options.css
│
├ manifest.json
2. UI構成（サイドパネル）

サイドパネルは以下のセクション構造。

kintone Base
  ├ ショートカット
  ├ ウォッチリスト
  ├ レコードピン
  └ 最近のレコード
3. UI設計ルール（統一）
セクション右上ボタン

右上アクションは 統一された順序 で配置する。

[追加] [更新] [検索] [折り畳み]

ただし機能が無い場合は省略可。

重要ルール

折り畳みは必ず右端

例

[＋] [↻] [🔍] [▼]
折り畳みボタン

折り畳みボタンは 小さい▼で統一。

NG

chevron icon

大きい矢印

セクションごとに異なる形

正
▼

必要なら

▼ / ▶
検索UI

検索は ポップオーバー型。

表示構造

検索ボタン
   ↓
popover
   ↓
候補リスト
4. アプリ検索
仕様

対象は アプリ名のみ

検索対象

App.name

検索方法

name.includes(query)
UI

検索は ショートカットアイコン列の右端ボタン。

[icon][icon][icon][icon][AppSearch]
検索結果

候補は

App name
app:123

最大表示

6〜8件
5. DOM更新ルール（重要）

hoverで再レンダリングは禁止

今回のバグ原因。

NG
mouseenter
↓
render()

理由

DOM差し替え
↓
click失敗
正しい方法
hover
↓
class toggle

例

updateActiveDom()
6. 検索操作

対応キー

ArrowUp
ArrowDown
Enter
Esc
7. 遷移処理

この拡張は Side Panel のため
通常の

window.location

は使用しない。

現在タブ
chrome.tabs.update()
新しいタブ
chrome.tabs.create()
8. 遷移設定（統一）

設定項目

ショートカット検索の開き方

設定キー

shortcutSearchOpenMode

値

current_tab
new_tab
適用範囲（統一）

この設定は すべてのショートカット遷移に適用。

対象

ショートカット検索
ショートカットアイコン

つまり

ショートカットの開き方

として動作。

9. 設定画面UI

設定画面は ミニマルUI を採用。

NG
大きいカード
正
横並びラジオ

例

(●) 現在タブ   ( ) 新しいタブ
10. データ保存

設定保存

chrome.storage.local
11. 権限処理

権限ロジックは

permission-service.js

へ分離。

content.jsは

permission service を呼ぶだけ
判定API
canEditRecord()
canDeleteRecord()
canAddRow()
canEditCell()
getCellPermission()
filterPasteTargets()
12. セキュリティ方針

編集不可条件

lookup field
system field
unknown field
readonly record
status locked
13. UIレスポンス

重要ルール

render最小化

再描画するタイミング

検索語変更
データ変更
表示切替

再描画しないタイミング

hover
focus
mousemove
14. CSS方針

CSSは

launcher.css

に集中。

クラス命名

kp-

例

kp-app-search-item
kp-section-actions
kp-collapse-btn
15. 今後の拡張のための原則

この拡張は ツール化する前提。

そのため

原則
UI一貫性
処理共通化
責務分離
共通関数化

URL遷移

openShortcutUrl()
サービス分離
permission-service
UI分離
launcher
16. UXルール

優先順位

速度
シンプル
視認性
UI基本方針
1操作1結果

例

検索 → Enter → アプリ
17. 今後追加予定（参考）

候補

Ctrl + K Launcher
Recent Apps
Pinned Apps
まとめ

この拡張の設計思想は

kintoneを高速操作するためのランチャー
特に重要な統一ルール
UI
折り畳みは右端
折り畳みは▼
検索はpopover
処理
hoverでrender禁止
遷移処理は共通化
設定
ショートカット遷移設定は共通適用